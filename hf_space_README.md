---
title: FeedbackOS API
emoji: 🎯
colorFrom: indigo
colorTo: purple
sdk: docker
pinned: false
---

# FeedbackOS API

FastAPI backend for FeedbackOS — rubric-based feedback standardization for 100xEngineers mid-capstone reviews.

## Endpoints

- `GET /health` — liveness check
- `GET /docs` — Swagger UI
- `GET /submissions` — list submissions
- `GET /reviews` — reviews
- `GET /rubrics` — rubrics
- Full API docs at `/redoc`
